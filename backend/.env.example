# Backend Environment Variables Template
# Copy this file to .env and fill in your actual values

# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-generate-with-python-secrets

# n8n Webhook Integration
# Get your webhook URL from n8n workflow
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/contact

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://yourusername.github.io

# Rate Limiting
# Max requests per hour per IP
RATE_LIMIT_PER_HOUR=5

# Database (if needed later)
# DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# Email Configuration (if using direct email instead of n8n)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Logging
LOG_LEVEL=INFO
